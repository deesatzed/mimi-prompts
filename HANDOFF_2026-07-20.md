# MiniPromptLib — Handoff Packet

**Generated:** 2026-07-20
**Branch:** `master` @ `84f3a7a`
**Last Commit:** `docs: describe rm/rename/edit, folders, harvest, and feedback in user docs`

---

## Quick Resume Checklist

- [ ] `cd /Volumes/WS4TB/mimi_prompt`
- [ ] `python3 -m venv .venv && . .venv/bin/activate`
- [ ] `python -m pip install .`
- [ ] `python3 -m unittest discover -s tests -v` — expect 140 passing tests.
- [ ] `python3 scripts/tabletop_demo.py --assert --output docs/reports/tabletop-results.json` — expect 10 passes and `llm_calls=0`.
- [ ] `python3 scripts/release_check.py` — expect `RELEASE CHECK PASS`.
- [ ] Smoke-check the core flow: `mini suggest --context "The build failed with a traceback"` — expect a numbered small page.
- [ ] Review **Current Blockers** before changing branch/remote or publishing.

## AI Continuity Checklist

- [x] This packet replaces the stale `HANDOFF_2026-07-19.md` pointer.
- [x] `GOAL.md`, `GOAL_GAP_FIX.md`, `UX_Gap_Analysis_2026-07-19.md`, prior handoffs, and current code were reviewed.
- [x] All 14 gaps from `UX_Gap_Analysis_2026-07-19.md` (G1–G14) were addressed or explicitly deferred with authorization required — see `GAP_FIX_REPORT_2026-07-20.md`.
- [x] Pre-existing untracked `idea2_app_directions.md` is preserved.
- [x] Fresh full verification suite executed on 2026-07-20, **after** this packet became `HANDOFF_LATEST.md` (per the process rule established in Phase 0 of the gap-fix work).
- [ ] Resolve `master` versus `main`: checkout is `master`; Pages triggers on `main`.
- [ ] Obtain explicit authorization before adding a remote, pushing, enabling Pages, or changing GitHub settings.

---

## What This Project Does

MiniPromptLib is an offline-first terminal navigator for AI-assisted work. Given bounded current context, it detects a workflow moment—uncertainty, failure, checkpoint, completion, and more—and offers at most three reusable mini-prompts. It balances workflow relevance with bounded `selection_count` frequency without presenting a huge menu; only an explicit numbered choice increments `selection_count`.

Since the 2026-07-19 packet, the app also: lets you view a suggestion without selecting it (`v N`) and undoes a selection's count when you back out of it (`b`); asks a one-keypress clarifying question instead of showing a confident-looking but essentially arbitrary page when nothing really matched; nudges (never saves) when typed context looks like a reusable rule; manages the library directly (`rm`/`rename`/`edit`); organizes prompts into an explicit folder taxonomy (`mini folders`); pulls reusable prompts out of pasted text with generalization, folder recommendation, and dedupe (`mini harvest`) — always preview-first, never auto-saving; and closes the usage-outcome loop (`mini feedback`, `mini stats`).

The installed `mini` command owns a local JSON prompt store and interaction loop. Host integrations are thin, project-local templates using the same JSON contract. The app does not read full conversation history, does not call a model for the core workflow, and does not auto-save, deploy, or alter global host settings.

**Tech Stack:** Python 3.11+, standard-library runtime, setuptools, `unittest`, dependency-free HTML/CSS/JavaScript.
**Architecture Pattern:** Local CLI and reusable Python engine with opt-in adapter templates; no server, database, account, telemetry, mandatory model, or network service.

---

## Project Structure

```text
mimi_prompt/
├── minipromptlib/               # Installable library and `mini` command
│   ├── cli.py                   # Interactive and JSON CLI surfaces
│   ├── navigator.py             # Select/view/more/retry/nest/back session state
│   ├── classifier.py            # Workflow-state inference
│   ├── ranking.py               # Relevance, bounded frequency ranking, weak-signal detection
│   ├── storage.py               # Schema-v3 atomic JSON persistence (additive folder field)
│   ├── capture.py               # Preview/confirmation single-prompt capture
│   ├── harvest.py               # Detect/generalize/file/dedupe multi-candidate pipeline
│   └── data/seeds.md            # Packaged reviewed 41-prompt panel (synced with root seeds.md)
├── tests/                       # 140 offline regression/behavior tests
├── scripts/                     # Tabletop and installed-package verification
├── docs/                        # Install, UX, hosts, plans, and reports
├── integrations/                # Copyable project-local host templates
├── site/                        # Static GitHub Pages landing artifact
├── .github/workflows/           # Verify and Pages workflow definitions
├── README.md                    # Public quick start and safety boundary
├── GOAL.md                      # Completion contract and authority order
├── GOAL_GAP_FIX.md              # Autonomous gap-remediation contract (this session's work)
├── UX_Gap_Analysis_2026-07-19.md   # Source of the 14 gaps this session addressed
├── GAP_FIX_REPORT_2026-07-20.md    # Per-gap completion report
└── pyproject.toml               # Package metadata and `mini` script
```

**Entry Points:**

- `mini` — installed console script (`minipromptlib.cli:main`).
- `python3 -m minipromptlib.cli` — canonical module CLI.
- `cli.py` — preserved direct-CLI compatibility surface.
- `scripts/tabletop_demo.py` — ten deterministic product scenarios.
- `scripts/release_check.py` — installed-package and artifact verification.

| Module | Path | Purpose | Status |
| --- | --- | --- | --- |
| Navigator | `minipromptlib/navigator.py` | Small pages, view, select, nest, back (with count-undo) | ✅ verified |
| Classifier/ranking | `classifier.py`, `ranking.py` | Workflow-first frequency-weighted suggestions; honest fallback reasons; weak-signal detection | ✅ verified |
| Storage | `storage.py` | Atomic local JSON, schema v3, fail-closed migration | ✅ verified |
| Harvest | `harvest.py` | Deterministic detect/generalize/file/dedupe pipeline | ✅ verified |
| Seeds | `seeds.py`, `data/seeds.md` | Parse/install 41 authored prompts across all workflow states | ✅ installed-package proof |
| Static site | `site/` | Public landing source | ✅ local artifact; ⚠️ not deployed |
| Host templates | `integrations/`, `docs/hosts/` | Bounded-context instructions | ✅ contract tested; ⚠️ not host-live-tested |

---

## How to Run

### Local Development

```bash
cd /Volumes/WS4TB/mimi_prompt
python3 -m venv .venv
. .venv/bin/activate
python -m pip install .
mini --storage "$PWD/.miniprompts/prompts.json" seed
mini --storage "$PWD/.miniprompts/prompts.json" suggest --interactive --context "Not sure, lets run through scenarios before deciding."
```

Expected: seeding reports 41 prompts; suggestion pages show one to three numbered choices and `v N`, `m`, `n`, `p`, `c`, `r`, `a`, `b`, `h`, and `q` actions.

### Tests

```bash
python3 -m unittest discover -s tests -v
```

**Current Status:** 140 passed, 0 failed, 0 skipped on 2026-07-20 (re-verified after this packet became `HANDOFF_LATEST.md`, per the Phase 0 process rule).

### Verification Suite

Run these commands in order:

1. `python3 -m unittest discover -s tests -v`
2. `python3 scripts/tabletop_demo.py --assert --output docs/reports/tabletop-results.json`
3. `python3 scripts/release_check.py`
4. `python3 -m py_compile core.py cli.py seeds.py minipromptlib/*.py scripts/*.py tests/*.py`
5. `git diff --check`

**Pass Condition:** 140 unit tests pass; tabletop gives 10 PASS results with `llm_calls=0`; release check prints `RELEASE CHECK PASS`; compilation and whitespace check exit zero.

---

## Current State Assessment

### What's Working ✅

- Everything from the 2026-07-19 packet, still verified: workflow-aware small pages, non-repeating `more`, retry, nesting, preview, composition, confirmed capture; relevance-first ranking; atomic fail-closed storage; packaged seed panel; public docs/site artifacts.
- New this session (`GOAL_GAP_FIX.md` phases 0–4, all gated at 100% before proceeding):
  - Honest ranking reasons — fallback candidates no longer claim a false state match.
  - `v N` view-without-selecting; `b` back now undoes the selection count it added.
  - Runnable non-interactive hints; `MINI_STORAGE` env var with a storage banner; `--expect` drift guard on `--choice`.
  - `mini rm`/`rename`/`edit`; seeds display their authored human title, not the raw slug.
  - Schema v3 folder taxonomy (additive, fail-closed migration) with an explicitly authored folder per seed; `mini folders`; `--folder` filtering.
  - `mini harvest`: deterministic detect → generalize → file (folder recommendation) → dedupe pipeline over caller-supplied text only; nothing saves without per-candidate consent.
  - `mini feedback`/`mini stats` wire the previously dormant `log_usage`/`get_underperforming_prompts` into a real loop.
  - Weak-signal clarify prompt: when the classifier lands on `general` and nothing on the shown page actually matches, the app asks instead of presenting a confident-looking arbitrary page.
  - Capture nudge in the interactive loop (asks once per session, never saves).
  - Seed panel rebalanced: `failure` grew from 2 to 6 seeds, `capture` from 0 to 3.

### What's Incomplete ⚠️

- GitHub publication has not happened: no remote configured. (Unchanged from prior packets — explicitly out of `GOAL_GAP_FIX.md`'s scope, requires separate authorization.)
- `Pages` triggers on `main`, but this checkout is `master`.
- Codex, Claude Code, and Cursor remain copyable project-local templates, not live-tested integrations.
- Optional LLM-assisted harvest/generalize pass (`GOAL_GAP_FIX.md` Phase 5) was not attempted — it requires the user to choose a model first, per workspace policy.

### What's Broken ❌

- No known broken local behavior from the fresh verification run.

### Current Blockers 🚧

- External publication actions still require explicit authorization (unchanged).
- LLM-assisted harvest features are blocked on a user-supplied model choice (unchanged policy: never a built-in default).

### Feature Completion Matrix

| Feature | Status | Evidence | Gap to Done | Priority |
| --- | --- | --- | --- | --- |
| Local workflow navigator (view/back-undo/help) | ✅ | `tests/test_navigator.py`, `tests/test_cli_ux.py` | None for stated local goal | P0 |
| Honest ranking + weak-signal clarify | ✅ | `tests/test_ranking.py`, `tests/test_weak_signal.py` | None | P0 |
| Library lifecycle (rm/rename/edit) | ✅ | `tests/test_lifecycle.py` | None | P0 |
| Folder taxonomy (schema v3) | ✅ | `tests/test_folders.py`, `tests/test_storage.py` | None | P0 |
| mini harvest | ✅ | `tests/test_harvest.py`, `tests/test_harvest_cli.py` | Optional LLM-assisted pass deferred | P1 |
| Feedback loop (mini feedback/stats) | ✅ | `tests/test_feedback.py` | None | P1 |
| Capture nudge | ✅ | `tests/test_capture_nudge.py` | None | P2 |
| Seed panel rebalance | ✅ | `tests/test_seeds.py` | None | P1 |
| GitHub Pages/public repository | ⚠️ | `.github/workflows/pages.yml` | Reconcile branch, add remote, push, enable/verify — requires authorization | P0 |
| Optional LLM-assisted harvest | ⚠️ | N/A — not started | Requires user-selected model | P2 |

---

## Recent Changes

| Date | SHA | Change | Why |
| --- | --- | --- | --- |
| 2026-07-20 | `84f3a7a` | Docs: describe rm/rename/edit, folders, harvest, feedback | Close-out for `GOAL_GAP_FIX.md` |
| 2026-07-20 | `5ad6a36` | Phase 4d: rebalance seed panel | `failure`/`capture` states had 2/0 seeds respectively |
| 2026-07-20 | `78ec20e` | Phase 4a-c: feedback loop, clarify fallback, capture nudge | Close G9, G6, design 4-C |
| 2026-07-20 | `9ee05ae` | Phase 3: folder taxonomy (v3) + mini harvest | Close G4 |
| 2026-07-20 | `4421475` | Phase 2: library lifecycle, human titles | Close G5, F12 |
| 2026-07-20 | `eb214ec` | Remove committed `.DS_Store` files | Repository hygiene |
| 2026-07-20 | `32e35d6` | Phase 1: truthfulness and safety fixes | Close G2, G3, G7, G8, G10, G12, G13 |
| 2026-07-20 | `0ac82b7` | Phase 0: restore doc verification gate | Close G1; add gap analysis and fix contract |
| 2026-07-19 | `d91f9da` | Public MiniPromptLib release preparation | Portable seeds, docs, site, host templates |

**Uncommitted Changes:** `idea2_app_directions.md` remains intentionally preserved and untracked. This packet and the `HANDOFF_LATEST.md` symlink flip are pending commit.
**Stashed Work:** None.

---

## Configuration & Secrets

Unchanged from prior packets: no environment variables, no external service required for the core product; `MINI_STORAGE` is a new optional override (not a secret). Optional Ollama/compatible model remains legacy-improvement/mining-only, do not invoke.

---

## Known Issues & Tech Debt

- [ ] Resolve the `master` checkout versus `main` Pages trigger before first public push.
- [ ] Optional LLM-assisted harvest/generalize pass is deferred pending user model choice.
- [ ] No lint or static type-check configuration exists; test, release, compile, and whitespace checks remain the quality gates.
- [ ] `mini capture` was deliberately kept as a separate, simpler path from `mini harvest` rather than unified, to avoid breaking its existing tested `--confirm`/`--json` contract used by host integrations — see `GAP_FIX_REPORT_2026-07-20.md` for the reasoning.
- [ ] Folder is not refreshed on re-seed (unlike title), by design — an existing prompt's folder is treated as legitimate user customization, not something re-seeding should silently override.

## Next Steps (Priority Order)

1. **Review the 7 new seed prompts** (numbers 35–41 in `seeds.md`) — author-reviewed but not yet user-reviewed. See `GAP_FIX_REPORT_2026-07-20.md` §4d for the verbatim text.
2. **Publish only with explicit authorization** — same as before: resolve branch policy, add remote, push, enable Pages.
3. **Decide the optional LLM-assisted harvest scope** — if wanted, specify which current model to use; the harness must not choose one.
4. **Validate one host template live** — unchanged from prior packets.

## Key Files Reference

| File | Purpose | When to Modify |
| --- | --- | --- |
| `GOAL_GAP_FIX.md` | This session's autonomous execution contract | Reference only; superseded once all boxes are checked |
| `UX_Gap_Analysis_2026-07-19.md` | Source of the 14 gaps addressed this session | Historical reference |
| `GAP_FIX_REPORT_2026-07-20.md` | Per-gap completion report | Reference only |
| `minipromptlib/harvest.py` | Detect/generalize/file/dedupe pipeline | Harvest behavior changes |
| `minipromptlib/storage.py` | Schema v3, migration | Schema/migration changes |
| `minipromptlib/ranking.py` | Relevance/frequency/weak-signal policy | Ranking policy changes |
| `seeds.md` / `minipromptlib/data/seeds.md` | Shipped authored panel (keep in sync) | Reviewed prompt changes |

## Open Questions / Decisions Needed

- Same publication questions as the prior packet (branch policy, authorization, first host to live-validate).
- Should the 7 new seed prompts (35–41) be revised after user review?
- Is the optional LLM-assisted harvest pass wanted, and if so, which model should be used?
